# Run 3 manuscript

This directory contains the journal-style working paper on
symmetry-/subalgebra-accessible observable contrast. It is a new manuscript,
not a silent revision of run 1 or run 2.

```bash
make
```

The build reuses the author's archived IEEE class. The layout is a working
format and does not imply submission or acceptance by IEEE.
